// Bundle entry for run-a-browser.mjs. This is the SHIPPED compiled code only, re-exported
// unchanged, so the browser runs exactly what the product would.
export { preprocessToTensor, HEAD_CONTRACT } from "../../../../packages/perception/dist/src/index.js";
